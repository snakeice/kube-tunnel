
[tasks.install-app]
description = "Install dependencies and build the project"
run = "go build -o ~/go/bin/kube-tunnel ./cmd"

[tasks.lint]
description = "Run linters"
run = "golangci-lint run"

[tools]
go = "1.26"
golangci-lint = "2.12"
pre-commit = "4.6.0"
